# ML layer deps — kept SEPARATE from app/requirements.txt so the Streamlit Cloud
# image stays lean. Pinned to the locally-verified versions (rigor-pass Area 7).
# Install into the project venv:  .venv/Scripts/pip install -r ml/requirements.txt
lightgbm==4.6.0
mlflow==3.14.0
scikit-learn==1.9.0
holidays==0.100
fastapi==0.139.0
uvicorn[standard]==0.51.0
# shared data stack (same pins as app/requirements.txt)
duckdb==1.5.4
pandas==3.0.3
pyarrow==24.0.0
numpy==2.5.1
