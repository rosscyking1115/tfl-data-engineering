# Prepared portfolio copy — do not publish from this workflow

## Website

I built a licence-bounded reliability-reference suite from constructed fixtures and a reviewed
semantic oracle. The portable `0.2.0` suite checks deterministic replay, replacement, rejection and
interruption recovery in DuckDB and a digest-pinned Spark 4.0.1 container. Do not add a Databricks
claim unless the managed gate records `PASS`.

## CV

- Built a constructed-fixture conformance suite for deterministic replay and atomic recovery.
  DuckDB and Spark match on semantic output, with faults injected at three publication boundaries.
- Optional after managed `PASS`: validated the same oracle once on bounded Databricks serverless
  Delta, then exported redacted evidence and verified targeted teardown.

## LinkedIn

The difficult part of ingestion starts when a source file is replayed, corrected, malformed or
interrupted just before publication. I turned five years of observed header drift into a small
constructed test pack for those cases. DuckDB and pinned Spark must agree on every decoded row,
disposition, reconciliation event and state hash.

Do not imply that the constructed incidents occurred in TfL data. Do not mention Databricks parity
unless the committed managed report records `PASS` and verified teardown.
