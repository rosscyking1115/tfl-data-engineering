# Pinned to the versions verified locally on Python 3.13.
# Streamlit Community Cloud: set Python version to 3.13 in Advanced settings
# (it does NOT read runtime.txt). Newer Python (3.14) breaks the pandas/pyarrow stack.
streamlit==1.59.1
duckdb==1.5.4
pandas==3.0.3
pyarrow==24.0.0
numpy==2.5.1
altair==6.2.2
# QA assistant ("Ask the data" page). The page stays inert unless ANTHROPIC_API_KEY
# is set — keep that key OUT of public Streamlit secrets (see ADR-0007).
anthropic==0.116.0
