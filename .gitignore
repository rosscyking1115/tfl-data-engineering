# Data — raw files never belong in git
data/

# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
logs/

# MLflow local tracking (the model + feature-importance artifacts under ml/model/ ARE committed)
mlflow.db
mlruns/
mlartifacts/

# OS / editor
.DS_Store
Thumbs.db

# dbt local user id
dbt/.user.yml

# Local Claude Code tooling config (preview launch, local settings)
.claude/
