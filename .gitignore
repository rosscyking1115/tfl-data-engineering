# Data — raw files never belong in git
data/

# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.benchmark-output/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/*.duckdb
dbt/*.duckdb.tmp/
dbt/.duckdb_tmp/
logs/

# MLflow local tracking (the model + feature-importance artifacts under ml/model/ ARE committed)
mlflow.db
mlruns/
mlartifacts/

# OS / editor
.DS_Store
Thumbs.db

# dbt local user id
dbt/.user.yml

# Local editor/agent tooling config (preview launch, local settings)
.claude/
